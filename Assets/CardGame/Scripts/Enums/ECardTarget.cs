namespace LichLord.CardGame
{
    /// <summary>
    /// Which dice pool a card effect reads when evaluating its trigger condition.
    /// </summary>
    public enum ECardTarget
    {
        PlayerDice,
        EnemyDice,
    }
}
